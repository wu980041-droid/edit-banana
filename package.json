{
  "name": "edit-banana",
  "version": "1.0.0",
  "description": "Edit Banana — Image/PDF to editable DrawIO XML pipeline",
  "private": true,
  "scripts": {
    "build": "pip install -r requirements.txt && python -m py_compile main.py && python -m py_compile server_pa.py",
    "start": "python server_pa.py"
  }
}
